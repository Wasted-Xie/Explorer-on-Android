# Explorer on Android 项目整体规划

## 项目背景
Windows Explorer 是市占率最高的桌面文件管理器，但在 Android 平台上缺乏类似的桌面环境体验。本项目旨在将 Windows Explorer 的核心交互和功能移植到 Android 平台，提供熟悉的文件管理体验，同时充分利用 Android 的触摸和多任务特性。

## 项目目标
1. 实现类似 Windows Explorer 的文件夹树、详细视图、右键（长按）菜单、拖拽操作等核心交互。
2. 支持基本文件操作：复制、剪切、粘贴、删除、重命名、新建文件/文件夹、属性查看。
3. 提供详细信息面板（预览、元数据）。
4. 支持多窗口/分屏模式（Android 平板和折叠屏设备）。
5. 集成 Android 存储访问框架（SAF），以访问外部存储、云存储等。
6. 提供自定义主题和视图选项（图标、列表、详细）。
7. 确保与 Android 安全模型兼容（分区存储、Scoped Storage）。
8. 性能流畅，支持大目录快速加载。

## 非功能需求
- 最低支持 Android 8.0 (API 26)。
- 使用 Kotlin 和 Jetpack Compose 构建现代 UI。
- 遵循 Material Design 3 指南。
- 采用 MVVM 架构，配合 Hilt 依赖注入和协程。
- 单元测试覆盖率 > 80%。
- 通过 Android App Bundle 发布，支持多架构（armeabi-v7a, arm64-v8a, x86_64）。

## 系统架构概览
```
+-------------------+      +-------------------+      +-------------------+
|   Presentation    |<---->|    Domain Layer   |<---->|    Data Layer     |
| (Jetpack Compose) |      | (UseCases, Model) |      | (Repositories,   |
|                   |      |                   |      |  Remote/Local)    |
+-------------------+      +-------------------+      +-------------------+
          ^                         ^                         ^
          |                         |                         |
      UI Events                 Business Logic           Data Sources
```

- **Presentation Layer**: 负责 UI 渲染、用户输入处理、状态观察。
- **Domain Layer**: 包含纯 Kotlin 的业务逻辑（Use Cases）、数据模型。
- **Data Layer**: 负责数据获取（本地文件系统、存储访问框架、网络缓存等），实现 Repository 接口。

## 功能模块划分
1. **核心文件浏览器**
   - 文件夹树导航（侧边栏）
   - 内容 pane（图标视图、列表视图、详细视图）
   - 路径面包屑导航
   - 排序和过滤
2. **文件操作**
   - 剪切/复制/粘贴（剪贴板）
   - 删除（送入回收站或直接删除）
   - 重命名
   - 新建文件/文件夹
   - 属性查看对话框
3. **右键（长按）菜单**
   - 基于选中项的动态菜单
   - 支持多选操作
4. **拖拽支持**
   - 内部拖拽（移动/复制）
   - 外部拖拽（分享、到其他应用）
5. **存储访问**
   - 内部存储（应用专属目录）
   - 外部存储（通过 SAF 访问）
   - 云存储插件预留接口（Google Drive、OneDrive 等）
6. **设置与个性化**
   - 主题（浅色/深色/跟随系统）
   - 视图偏好（默认视图、列宽、排序方式）
   - 高级选项（显示隐藏文件、文件扩展名）
7. **性能与后台**
   - 异步目录加载（协程 + 分页）
   - 缓存最近访问的目录
   - 前台服务处理长时间操作（大文件复制）

## 技术栈
- **语言**: Kotlin 1.8+
- **UI**: Jetpack Compose + Material3
- **架构**: MVVM + Clean Architecture
- **依赖注入**: Hilt
- **异步**: Kotlin Coroutines + Flow
- **数据持久**: DataStore (偏好设置)、Room (可选，用于历史记录)
- **存储访问**: Android Storage Access Framework (SAF)
- **文件操作**: java.nio.file、AutoFile (第三方) 或自定义封装
- **测试**: JUnit5, Mockito, Turbine (Flow测试), Compose Test
- **CI/CD**: GitHub Actions (构建、测试、生成 AAB)

## 项目里程碑
| 里程碑 | 目标 | 预计时长 |
|--------|------|----------|
| M0: 项目初始化 | 创建仓库、配置基础构建、搭建 Hello World Compose 应用 | 1周 |
| M1: 基础文件列展示 | 实现内部存储目录递归遍历，展示图标列视图 | 2周 |
| M2: 导航栏与路径面包屑 | 添加侧边栏文件夹树、顶部路径导航 | 2周 |
| M3: 基本文件操作（复制/粘贴/删除/重命名） | 实现剪贴板操作、UI 提示、撤销/恢复（可选） | 3周 |
| M4: 长按菜单与多选 | 实现长按弹出上下文菜单、多选状态、批量操作 | 2周 |
| M5: 视图切换（图标/列表/详细） | 支持不同布局模式、详细信息面板 | 2周 |
| M6: 存储访问框架集成 | 支持访问外部存储、USB OTG、云存储（预留接口） | 3周 |
| M7: 拖拽支持 | 实现内部拖拽移动/复制、基本外部拖拽分享 | 2周 |
| M8: 设置与个性化 | 添加设置页面、主题切换、视图偏好持久化 | 1周 |
| M9: 性能优化与测试 | 加载优化、缓存、单元/UI 测试补全、Bug 修复 | 3周 |
| M10: 准备发布 | 生成签名 APK/AAB、撰写隐私政策、内测准备 | 2周 |
| **总计** | 约 **24周**（约 6个月） |  |

## 风险与应对
- **Scoped Storage 限制**：使用 SAF 和系统授权获取访问权限；在说明页引导用户授予“所有文件访问”权限（Android 13+ 需要特殊声明）。
- **后台执行限制**：长时间文件操作使用前台服务并在通知中显示进度。
- **兼容性**：适配不同屏幕尺寸、折叠屏、桌面模式（通过窗口类别 API）。
- **性能**：目录加载使用协程分页、LruCache 缓存图标和元数据。

## 下一步行动
1. 创建 Git 仓库并初始化项目结构。
2. 搭建基本的 Compose Activity 和导航图。
3. 实现文件系统抽象层（Repository 接口）。
4. 开始迭代 M0 与 M1 任务。

祝项目成功！